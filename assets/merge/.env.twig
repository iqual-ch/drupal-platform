PROJECT_CODE_NAME={{ name }}

{% if deployment == "platform.sh" %}
# Platform.sh and Upsun config
PLATFORMSH_PROJECT_ID={{ platformsh_config.project_id }}
{% endif %}

# Drupal single point of truth for db/fs
DRUPAL_SPOT={{ drupal_spot }}
